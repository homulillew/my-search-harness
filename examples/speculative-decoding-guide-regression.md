# 推测解码：大语言模型推理加速的方法与边界

## 基本机制与收益条件

大语言模型（Large Language Model，LLM）的自回归生成通常一次只确定一个新词元（token）。在批次较小、推理受内存带宽限制时，模型权重读取和串行依赖使并行硬件难以被充分利用。推测解码（Speculative Decoding）的基本做法是：先由低成本的草稿模型（draft model）顺序提出一段候选词元，再由昂贵的目标模型（target model）在一次并行前向计算中同时评分这些候选。系统从左到右接受候选；首次拒绝时丢弃后续候选，并从目标模型与草稿模型的残差分布中重新采样；如果候选全部被接受，还可以从目标模型分布追加一个奖励词元。该接受—拒绝过程保证最终输出仍服从原目标模型的采样分布，而不是用近似模型替代目标模型。[1, section: Speculative Decoding]

实际加速取决于每轮验证平均能够提交多少词元，以及草稿模型相对目标模型的计算成本。Leviathan 等在 T5-XXL、单 TPU、批次大小为 1 的翻译与摘要实验中报告了约 2.3–3.4 倍的实际运行时间加速。更大的草稿模型虽然可能提高接受率，却也可能因为自身计算成本增加而降低整体速度。[1, section: Experiments]

## 三类方法及其边界

### 保持目标分布的草稿—验证方法

这类方法只改变执行计划，不改变目标模型的输出分布。它不要求修改目标模型架构或重新训练，并且即使所有草稿候选都被拒绝，每轮仍至少提交一个由目标模型生成的词元。代价是，较低的接受率会浪费候选序列后部的计算；如果硬件没有足够的并行计算余量，额外验证也未必能够降低延迟。原论文将束搜索、定制草稿模型、分层推测、动态调整草稿模型或候选长度，以及向其他模态扩展列为后续方向。[1, section: Discussion]

### 推测式对比解码

推测式对比解码（Speculative Contrastive Decoding，SCD）不再以原专家模型分布作为唯一目标，而是结合较小模型与专家模型的 logits 构造对比分布，再用推测式接受过程加速这个新目标。简单词元可以快速通过，遭到拒绝的困难词元则接受对比修正，因此该方法同时讨论生成质量与效率，而不是只复现专家模型的输出。[2, section: Speculative Contrastive Decoding]

在 Llama-2 7B/70B 以及 WikiText、GSM8K、HumanEval、AlpacaEval 的实验设置中，原始 SCD 的 HumanEval Pass@1 为 37.20，而 70B 对照为 28.66；改进版 SCD 在 WikiText 上取得 15.20 的困惑度。不过，改进版在 HumanEval 和 AlpacaEval 上明显弱于原始版本，说明不同任务上不存在统一占优的对比解码实现。[2, section: Experiments] 其速度还共同受接受率、小模型与专家模型的成本比以及超参数影响；当所有候选都被接受时，系统仍需执行一次额外的小模型前向计算。[2, section: Analysis]

### 基于推测的协作解码

基于推测的协作解码（Collaborative Decoding via Speculation，CoS）面向集成生成、对比解码等多模型协作场景。它先明确组合分布，再用候选分布对该组合分布执行具有正确性保证的接受过程与残差重采样。交替候选框架还会把验证模型产生的奖励词元交给下一轮反向验证，使两个模型轮流担任候选生成者，并可进一步推广到多个模型。[3, section: Collaborative Decoding via Speculation]

相对于标准协作解码，论文中的加权集成 CoS 获得约 1.27–1.85 倍加速，对比解码 CoS 获得约 1.11–2.23 倍加速。这些数字不能直接解释为相对于单个目标模型自回归生成的收益，因为实验基线本来就需要调用多个模型。实验还表明，把普通推测解码直接套用于协作场景并不保证加速；接受率较低时，速度可能降至基线的 0.92–0.98 倍。[3, section: Experiments]

## 方法比较与工程权衡

| 方法 | 输出分布 | 主要收益 | 关键限制 |
|---|---|---|---|
| 保持分布的草稿—验证 | 原目标模型分布 | 减少串行调用目标模型的次数 | 需要廉价、匹配的草稿模型和足够的并行资源 |
| SCD | 专家模型与较小模型构成的对比分布 | 同时进行质量修正和推测式加速 | 改变目标分布，质量与速度对任务和超参数敏感 |
| CoS | 明确定义的多模型组合分布 | 加速已有的集成或对比协作 | 收益取决于比较基线、模型成本与组合函数 |

三类方法共同受接受率、候选生成成本和候选长度 γ 控制。增大 γ 会提高每轮可能提交的词元数，但序列后部的词元依赖尚未验证的前缀，发生拒绝时会扩大无效计算。因此，最优候选长度必须结合具体模型组合、任务和硬件测量。吞吐量、单请求延迟、额外浮点运算量、显存占用和并发余量也不能被一个加速倍数替代。

## 未决问题

一个重要问题是，系统能否根据输入难度、实时接受率、草稿模型与目标模型的成本以及硬件负载，动态选择候选模型和候选长度，同时明确声明输出究竟服从原目标分布、对比分布还是组合分布。Leviathan 等对动态和分层候选机制的讨论，以及 CoS 对候选长度的敏感性结果，都表明静态配置很难覆盖所有部署条件。

另一个问题是，当宽松接受、对比解码或模型集成主动改变输出分布时，应如何设计跨任务评测，同时报告生成质量、实际运行时间、额外计算量与硬件条件，避免把特定基准上的收益推广为普遍结论。三篇论文采用的任务、设备和比较基线差异明显，现有证据尚不足以给出统一答案。

## 结论

推测解码不是单一算法，而是一组以低成本候选生成换取高成本验证并行度的方法。选择方案前，应先固定输出分布的语义和比较基线，再围绕接受率、模型成本比、候选长度与部署硬件优化。否则，“更快”可能来自改变生成目标或更换比较基线，而不是对同一推理任务实现无损加速。

## 参考文献

1. Yaniv Leviathan, Matan Kalman, Yossi Matias “Fast Inference from Transformers via Speculative Decoding.” 2022 arXiv 2211.17192; https://arxiv.org/abs/2211.17192
2. Hongyi Yuan, Keming Lu, Fei Huang, Zheng Yuan, Chang Zhou “Speculative Contrastive Decoding.” 2023 arXiv 2311.08981; https://arxiv.org/abs/2311.08981
3. Jiale Fu, Yuchu Jiang, Junkai Chen, Jiaming Fan, Xin Geng, Xu Yang “Fast Large Language Model Collaborative Decoding via Speculation.” 2025 arXiv 2502.01662; https://arxiv.org/abs/2502.01662
